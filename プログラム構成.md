# イヤホンガイドシステム - プログラム構成

## 概要

本システムは、リアルタイム音声翻訳機能を備えたツアーガイドシステムです。AWS Amplifyを基盤とし、React（フロントエンド）とAWS Lambda（バックエンド）で構成されています。

## システムアーキテクチャ

### 技術スタック

#### フロントエンド
- **React 18.3.1** - UIフレームワーク
- **React Router 6.26.2** - ルーティング
- **AWS Amplify 6.6.2** - AWS統合
- **Amazon Chime SDK JS 3.24.0** - 音声/ビデオ通信
- **i18next** - 多言語対応（日本語・英語）
- **Bootstrap 5.3.2** - CSSフレームワーク

#### バックエンド
- **AWS Lambda** - サーバーレス関数
- **AWS API Gateway** - REST API & WebSocket API
- **AWS Chime SDK** - 会議・メッセージング
- **AWS Cognito** - 認証・認可
- **AWS S3 & CloudFront** - ファイルストレージ・配信
- **AWS CloudWatch** - ログ管理

## ディレクトリ構造

```
earphoneprj/
├── src/                      # Reactフロントエンド
│   ├── components/           # UIコンポーネント
│   │   ├── admin/            # 管理者画面
│   │   ├── chat/             # チャット機能
│   │   ├── common/           # 共通コンポーネント
│   │   ├── guide/            # ガイド機能
│   │   └── viewer/           # ビューワー機能
│   ├── apis/                 # API通信
│   ├── hooks/                # カスタムフック
│   ├── services/             # サービスレイヤー
│   └── utils/                # ユーティリティ
│
└── amplify/                  # バックエンド
    ├── backend.ts            # バックエンド定義
    ├── configs/              # 設定ファイル
    └── functions/            # Lambda関数
```

## 主要モジュール

### 1. ミーティング管理（Meetings）
AWS Chimeを使用した音声通信機能を提供します。

#### Lambda関数
- `create-meeting` - 新規ミーティング作成
- `get-meeting` - ミーティング情報取得
- `create-attendee` - 参加者追加
- `list-attendee` - 参加者一覧取得
- `start-meeting-transcription` - 文字起こし開始

#### 主な機能
- リアルタイム音声通信
- 参加者管理
- 音声の文字起こし

### 2. チャンネル管理（Channels）
AWS Chime Messagingを使用したチャット機能を提供します。

#### Lambda関数
- `create-app-instance-user` - チャットユーザー作成
- `create-channel` - チャンネル作成
- `add-channel-membership` - メンバー追加
- `send-channel-message` - メッセージ送信
- `list-channel-membership` - メンバー一覧取得

#### 主な機能
- グループチャット
- リアルタイムメッセージング
- メンバー管理

### 3. ツアー管理（Tours）
ツアーの作成・管理・運営を行います。

#### Lambda関数
- `create-tour` - ツアー作成
- `create-batch-tour` - 一括ツアー作成
- `get-tour` - ツアー詳細取得
- `list-tour` - ツアー一覧取得
- `update-tour` - ツアー更新
- `delete-tour` - ツアー削除
- `calculate-max-connection` - 最大接続数計算
- `get-tour-by-number-and-date` - ツアー番号と日付で検索
- `get-meeting-by-tourid` - ツアーに関連するミーティング取得
- `update-meeting-by-tourid` - ツアーのミーティング情報更新

#### 主な機能
- ツアー情報の管理
- CSVによる一括登録
- QRコード生成
- 接続履歴管理

### 4. 翻訳機能（Translates）
リアルタイム音声翻訳を提供します。

#### REST API
- `translate-text-speech` - テキスト音声翻訳（同期処理）

#### WebSocket API
リアルタイム翻訳のための双方向通信：
- `connect` - WebSocket接続確立
- `disconnect` - 接続切断
- `select-language` - 言語選択
- `translate-audio` - 音声翻訳
- `connect-state` - 接続状態管理

#### 主な機能
- リアルタイム音声翻訳
- 多言語対応
- WebSocketによる低遅延通信

### 5. ユーザー管理（Users）
管理者ユーザーの認証・認可を管理します。

#### Lambda関数
- `create-admin` - 管理者作成
- `get-admin` - 管理者情報取得
- `list-admin` - 管理者一覧取得
- `update-admin` - 管理者情報更新
- `delete-admin` - 管理者削除
- `active-admin` - 管理者の有効/無効化
- `login` - ログイン認証
- `check-auth` - 認証チェック
- `refresh-token` - トークン更新

#### 主な機能
- JWT認証
- 管理者権限管理
- セキュアなトークン管理

### 6. ファイル管理（Upload）
S3を使用したファイルアップロード・配信を提供します。

#### Lambda関数
- `upload` - ファイルアップロード用の署名付きURL生成
- `view` - ファイル表示用の署名付きURL生成（CloudFront経由）

#### 主な機能
- セキュアなファイルアップロード
- CloudFrontによる高速配信
- 一時的なアクセス制御

### 7. ログ管理（CloudWatch Logs）
システム全体のログを集約管理します。

#### Lambda関数
- `add-cloud-watch-logs` - CloudWatchへのログ送信

## API構成

### REST APIs
- `/meetings` - ミーティング関連
- `/app-instance-users` - チャットユーザー関連
- `/channels` - チャンネル関連
- `/tours` - ツアー関連
- `/users` - ユーザー管理
- `/translate-text-speech` - 同期翻訳
- `/uploads` - ファイルアップロード
- `/cognito-login` - Cognito認証
- `/cloud-watch-logs` - ログ送信

### WebSocket API
- `wss://[domain]/` - リアルタイム翻訳用WebSocket

## セキュリティ

1. **認証・認可**
   - AWS Cognito による認証
   - JWT トークンによるAPI保護
   - 管理者権限の階層管理

2. **データ保護**
   - S3の署名付きURL
   - CloudFront による配信制御
   - CORS設定による アクセス制御

3. **通信の暗号化**
   - HTTPS/WSS による暗号化通信
   - セキュアなWebSocket接続

## 主要な画面構成

### 管理者向け
- ログイン画面
- ツアー管理（一覧・作成・編集）
- ユーザー管理（一覧・作成・編集）
- QRコード生成

### ガイド向け
- ツアー開始画面
- メインガイド画面（音声配信）
- サブガイド画面（補助）
- ファイルアップロード

### 参加者向け
- 言語選択画面
- ライブビューワー（音声受信）
- 日本語音声再生
- 多言語音声再生

## 最新の音声品質改善機能

### 2024年度追加実装

#### 1. **日本語音声の音量・音質改善**
**実装箇所**: `src/components/viewer/LiveViewerJa.js`

- **スピーカー選択機能**: AWS Chime SDKによる自動音声出力デバイス選択
- **デバイス変更検知**: イヤホン抜き差し時の自動切り替え対応
- **デフォルト音量最大化**: 初期音量100%設定
- **音量スライダーUI**: ユーザーによる0-100%音量調整
- **ミュート制御改善**: React状態とDOM同期の修正

#### 2. **iPhone SE対応の音声処理最適化**
**実装箇所**: `src/components/guide/StartLiveSession.js`, `src/components/guide/LiveSubSpeaker.js`

- **デバイス検知機能**: iPhone SE/低スペック端末の自動判定
- **Voice Focus無効化**: 低性能デバイスでのCPU負荷軽減
- **マイク許可タイムアウト延長**: iPhone SE向けに15秒に延長
- **AudioContext suspension対応**: iOS Safari向けの音声コンテキスト復旧

#### 3. **技術的改善詳細**

**デバイス検知ロジック**:
```javascript
const isLowEndDevice = () => {
  const isIPhoneSE = /iPhone SE|iPhone 6|iPhone 7|iPhone 8/.test(navigator.userAgent);
  const isOldAndroid = /Android [4-6]/.test(navigator.userAgent);
  const isLowMemoryDevice = navigator.deviceMemory && navigator.deviceMemory < 4;
  return isIPhoneSE || isOldAndroid || isLowMemoryDevice;
};
```

**音声デバイス自動選択**:
```javascript
const observer = {
  audioOutputsChanged: async (freshAudioOutputDeviceList) => {
    if (freshAudioOutputDeviceList.length > 0) {
      await meetingSession.audioVideo.chooseAudioOutput(
        freshAudioOutputDeviceList[0].deviceId
      );
    }
  }
};
```

**音量制御強化**:
```javascript
const [volume, setVolume] = useState(100); // デフォルト100%
audioElement.volume = volume / 100; // 0.0-1.0の範囲で設定
```

#### 4. **パフォーマンス最適化**

- **Voice Focus条件分岐**: 高性能デバイスのみで有効化
- **WebAudio処理軽量化**: 低スペック端末での処理負荷軽減
- **メモリ効率改善**: 4GB未満メモリ端末での最適化

これらの改善により、以下の問題が解決されました：
- スマートフォンでの音量小問題
- イヤホン抜き差し時の音声停止
- iPhone SEでのガイド音声問題
- iOS Safariでの音声コンテキスト問題

## 開発環境

- Node.js 環境
- TypeScript 5.6.2
- AWS CDK 2.160.0
- React Scripts (Create React App)

## デプロイ

AWS Amplify を使用した自動デプロイ：
1. GitLabへのプッシュ
2. Amplify によるビルド
3. CloudFormation によるインフラ更新
4. S3/CloudFront へのフロントエンドデプロイ